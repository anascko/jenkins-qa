parameters:
  aptly:
    server:
      mirror:
        salt-latest-xenial:
          source: http://repo.saltstack.com/apt/ubuntu/16.04/amd64/latest/
          distribution: xenial
          components: main
          architectures: amd64
          gpgkeys:
            - DE57BFBE
          publisher:
            component: salt-latest
            distributions:
              - xenial/nightly
            architectures:
              - amd64
