parameters:
  aptly:
    server:
      mirror:
        salt-latest-trusty:
          source: http://repo.saltstack.com/apt/ubuntu/14.04/amd64/latest/
          distribution: trusty
          components: main
          architectures: amd64
          gpgkeys:
            - DE57BFBE
          publisher:
            component: salt-latest
            distributions:
              - trusty/nightly
            architectures:
              - amd64
